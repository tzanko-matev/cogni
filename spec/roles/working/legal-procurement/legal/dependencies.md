# Dependencies

## Runtime dependencies

- Go-built `cogni` binary
- git (MVP VCS)
- ripgrep (`rg`) for search
- OpenRouter API access and credentials
- A web browser to view `report.html`

## Build dependencies

- Go 1.22+ toolchain
