# Backup and Restore

## Backup strategy

- Back up the `output_dir` directory to retain `results.json` and reports.

## Restore procedure

- Restore the `output_dir` folder from backup.
- Regenerate reports with `cogni report` if needed.
